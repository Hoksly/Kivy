<FolderWidget>:

    l1: l1

    Button:
        pos: self.parent.pos
        size: self.parent.size



        Image:
            source: 'sources/gg.png'
            size_hint: None, None
            height: self.parent.height
            width: self.height
            pos: (root.x + 10, root.y)

        Label:
            id: l1
            # text: root.txt doesn't work as it should work
            text: root.txt
            size_hint: None, None
            height: self.parent.height
            width: self.parent.width - self.parent.height * 1.5
            text_size: self.size
            halign: "left"
            valign: "middle"
            pos: (self.parent.x + self.parent.height * 1.5 , self.parent.y)


<PageWidget>:



<BoxLayout>:
    orientation: 'vertical'


